# SwarmHaul — Week 2 Update (2026-04-17)

Submitted via the SWARM CLI:

```sh
swarm update product
```

Paste the block below into the prompt — swap `<VIDEO_URL>` for the YouTube link first.

```txt
SwarmHaul — Week 2 Product Update What we shipped: Reputation economics layer — softened α=0.7 payment split, bounded γ=0.08 formation nudge, first-meeting Sybil ceiling at 0.6. White paper with prior-art critique, game-theoretic reading, and calibration strategy lives in the repo. Interactive Observatory — live α/γ sliders driving a real payment simulator, four canonical scenario projections (good citizen / burnout / breach / realistic lifecycle), asymmetry table across score levels. Every curve comes from /reputation-model/* endpoints so MCP tooling shares the same source of truth. Wallet-signed dispatch — shipper's Phantom signs list_package and pays the escrow directly on devnet. API never impersonates. Split into POST /packages/build-tx + /packages/confirm. Swarm Inspector — new detail view opens from map markers or Observatory ledger rows. Per-leg coloured polylines on the map, hand-off markers between segments, on-chain tx links per leg. Dashboard readability overhaul — killed ~90 low-contrast grey sites and every italic body/data render. Every primary value now passes WCAG AA on void. End-to-end on-chain validated — 3 packages listed on-chain, 3 agents reasoned via LLM (LiteLLM gpt-oss-120b), swarms formed on-chain, legs confirmed, 4.7 SOL settled with real explorer tx links. Route optimizer rewritten O(n³) → O(n²) — 1000 bids in 58ms (was timeout). Precomputed adjacency graph + cost-pruned BFS. 66 new unit tests. 200+ total tests — 24 API integration tests via testcontainers + ephemeral Postgres. Semgrep SAST added to CI alongside Gitleaks. Stress test: 1,443 req/s at 50 concurrent, p99 = 63ms. Live on devnet — program deployed at GW9wYUcfa6LT5vxJ12aN7nu8VxWVrM53jaZcrZak41sg. Public URLs at dashboard.swarmhaul.defited.com and api.swarmhaul.defited.com, three always-on reasoning agents (low/mid/high reputation) running on Orca, auto-deployed via GitHub Actions + webhook on every push to main. Commit hash surfaced on /health and in the dashboard chrome so you can see which build is live. This week's focus: Privy embedded wallets to remove the Phantom-install gate for normal users (Phantom stays as power-user option), public MCP endpoint at mcp.swarmhaul.defited.com so judges can plug Claude or Cursor into the live protocol and watch an LLM use the 8 tools end-to-end, and on-chain reputation PDA so peer attestations become first-class verifiable credentials — one agent pubkey signing a claim about another, anchored in a PDA the protocol can read. That's the DID + VC primitive the reputation model already assumes, finally on-chain. Video: <VIDEO_URL> | GitHub: https://github.com/mighty840/swarmhaul | Pitch: https://mighty840.github.io/swarmhaul-pitch/ | Observatory: https://dashboard.swarmhaul.defited.com
```

## Links referenced in the body

- **Observatory:** <https://dashboard.swarmhaul.defited.com>
- **API:** <https://api.swarmhaul.defited.com/health>
- **Program on devnet:** [`GW9wYUc…41sg`](https://explorer.solana.com/address/GW9wYUcfa6LT5vxJ12aN7nu8VxWVrM53jaZcrZak41sg?cluster=devnet)
- **Video:** _fill in after upload_
- **White paper:** [`docs/reference/reputation-economics.md`](../reference/reputation-economics.md)
- **Video shot-list:** [`2026-04-17-video-script.md`](./2026-04-17-video-script.md)
