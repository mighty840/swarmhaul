services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: swarmhaul
      POSTGRES_PASSWORD: swarmhaul
      POSTGRES_DB: swarmhaul
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
